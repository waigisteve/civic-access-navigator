# Data Placeholders

This directory is reserved for trusted, curated project data.

Suggested usage:

- `raw/` for untouched source materials
- `curated/` for cleaned and approved content used by the app
- `samples/` for small demo datasets

Do not place sensitive personal data here.
